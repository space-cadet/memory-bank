# Session: 2026-06-25 Night

**Started**: 2026-06-25 20:00:00 IST
**Focus Task**: T21: Database-Native Memory Bank Update Workflow
**Status**: 🔄 ACTIVE

## Work Done

### T21: Full Schema Fix Sweep (Phase F Continuation)

Continued from afternoon session — completed the full schema consistency fix across mb-core.

#### Fixes Applied

1. **Canonical schema.sql** (`memory-bank/database/schema.sql`):
   - `updated` → `last_updated`
   - `date` → `session_date`
   - `period` → `session_period`
   - `focus` → `focus_task`
   - Added missing `task_subtasks` table

2. **Canonical lib files** (`memory-bank/database/lib/`):
   - `inserts.js`: All SQL and function params updated to canonical names
   - `workflow.js`: All session queries and object references updated
   - `regenerate.js`: All task/session queries and markdown generation updated

3. **Parser files** (`memory-bank/database/`):
   - `parse-tasks.js`: `last_updated` in schema and INSERT
   - `parse-sessions.js`: `session_date`, `session_period`, `focus_task` throughout

4. **Test data**:
   - `test-schema.js`: Fixed `session_cache` INSERT to match actual schema columns
   - `generate-test-data.js`: Updated `sessionCache` object to canonical keys

5. **CLI commands** (`mb-cli/src/commands/`):
   - `task.js`: `updated` → `last_updated` in SELECT, UPDATE, and object creation
   - `session.js`: `date`/`period`/`focus` → `session_date`/`session_period`/`focus_task` throughout

6. **Template files** (`mb-cli/templates/`):
   - `schema.sql`: Added missing `task_subtasks` table
   - Copied fixed canonical versions of `regenerate.js`, `parse-tasks.js`, `parse-sessions.js`

7. **Documentation**:
   - Created `schema-protocol-reference.md` — single source of truth for v1.1 naming
   - Created `schema-audit-2026-06-25.md` — full file-by-file audit report
   - Updated `skills/mb-db-workflow/references/schema.sql` to v1.1

8. **Cleanup**:
   - Deleted stale `mb-cli/src/server-package/` (pre-sql.js snapshot with old names)
   - Deleted `mb-cli/src/sync-database-template.js` (would overwrite fixes)
   - Removed `sync:database-template` script from `mb-cli/package.json`

#### Verification

- `init-schema.js` test: **PASSED** — 10 tables, 23 indexes, all canonical columns confirmed
- Schema now consistent across all copies in mb-core

## Decisions

- v1.1 naming convention ratified as canonical: `last_updated`, `session_date`, `session_period`, `focus_task`, `content`
- Old names (`updated`, `date`, `period`, `focus`, `notes`) are forbidden — any file using them is a bug
- Stale `server-package/` approach abandoned; templates are now the single source of truth

## Next Steps

1. Commit all changes
2. Resume T21 Phase F: record-only mode, backfill tool, safe migration strategy
3. Propagate fixed templates to downstream repos (10 projects from mb-hygiene)
