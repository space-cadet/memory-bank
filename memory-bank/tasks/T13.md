# T13: Implement Memory Bank CLI

*Created: 2025-05-17*
*Last Updated: 2026-05-22 20:18:36 IST*

**Status**: 🔄 **IN_PROGRESS**
**Priority**: high

## Details

[Details](tasks/T13.md) - Parser execution & modular refactor (90% complete)

Implemented mb task, mb session, and mb update commands

Core commands implemented: mb task, mb session, mb update. mb file pending.

Implemented mb task, mb session, and mb update CLI commands. Fixed inserts.createSession() id bug. Updated CLI docs.

Phase E fresh-project validation (2026-05-20 to 2026-05-21):
- Added shared `findDbPath()` helper back into `mb-cli/src/commands/db.js` so `mb db query`, `mb db test`, and `mb db workflow` can resolve project databases correctly
- Extended `mb init --database` file copying so generated projects receive `schema.sql` and `test-workflow.js`
- Hardened generated workflow libraries so fresh projects support repeat `mb db test` runs, same-period `mb workflow` reuse, and direct `completeSessionWork()` calls
- Verified the corrected flow in the real sibling fixture at `/Users/deepak/code/memory-bank-test`

2026-05-22 CLI hardening continuation:
- Added record-only and regenerate-only workflow execution modes to `mb workflow` / `mb db workflow`, with combined mode only when explicitly requested
- Added `mb db sync` for upgrading existing generated projects from current canonical database templates/runtime files
- Verified workflow split behavior and generated-project sync path in the sibling fixture

## Subtasks

- [x] Evaluate options (Commander.js, Yargs, etc.)
- [x] Selected Commander.js for simplicity
- [x] Document selection rationale
- [x] Set up project structure
- [x] Create command routing
- [x] Basic error handling setup
- [x] Project validation
- [x] Directory creation
- [x] Template setup with database files
- [x] Fixed package.json with proper JSON structure
- [x] Added .env with SQLite configuration
- [x] Added schema.prisma template
- [x] Added pnpm-lock.yaml template
- [x] Created memory-bank/ README.md with onboarding
- [x] Created database/ DATABASE_README.md with full documentation
- [x] Implemented existing memory bank detection with --force option
- [ ] mb task commands
- [ ] mb session commands
- [ ] mb file commands
- [x] Restore shared DB path discovery for `mb db` subcommands
- [x] Ensure generated DB packages include `schema.sql`
- [x] Ensure generated DB packages include `test-workflow.js`
- [x] Finish fresh-project CLI flow without manual template/schema intervention

## Dependencies

- T12
