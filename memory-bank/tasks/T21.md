# T21: Database-Native Memory Bank Update Workflow

*Created: 2025-11-13*
*Last Updated: 2026-05-22 20:18:36 IST*

**Status**: 🔄 **IN_PROGRESS**
**Priority**: high

## Details

[Details](tasks/T21.md) - Phase E isolated-project validation completed its core hardening pass on 2026-05-21. The DB-native workflow now supports fast append-style recording via `mb workflow`, repeated same-period session reuse, fresh-project template bootstrap, and direct `completeSessionWork()` usage without manual DB setup.

Phase E outcomes:
- Verified real sibling-project bootstrap and workflow execution in `/Users/deepak/code/memory-bank-test`
- Fixed generated-project bootstrap so `mb init --database` now copies `schema.sql` and `test-workflow.js`
- Fixed DB path discovery for `mb db` subcommands and workflow completion helpers
- Fixed sql.js `:memory:` handling so repeat test runs no longer persist a literal `:memory:` file
- Added dirty-write gating so read-only DB opens do not overwrite newer writes on close
- Fixed repeated same-period workflow reuse so `sessions.focus`, `session_cache`, and regenerated session files stay synchronized
- Made `recordSessionWork()` auto-create missing task rows so rapid logging works in fresh projects with minimal input
- Made `completeSessionWork()` resolve the project DB path automatically and update the focused task status during explicit session completion
- Re-ran `mb db test` twice in the real sibling fixture and confirmed both passes succeed

Open follow-up:
- The core Phase E blockers are resolved, but the broader T21 task still has remaining long-term design/documentation work beyond this hardening pass
- 2026-05-22 follow-up: split `mb workflow` into explicit record/regenerate modes and added `mb db sync` for durable generated-project template/runtime refresh; importer/viewer schema drift fixes and sql.js dirty-write persistence fix were also landed. Remaining follow-up is focused `--db <path>` handling cleanup across CLI invocations.

## Subtasks

- [x] Create isolated sibling fixture project for Phase E
- [x] Create detailed Phase E test log
- [x] Validate fresh-project bootstrap path
- [x] Fix missing `schema.sql` copy during `mb init --database`
- [x] Fix missing `findDbPath()` helper in `mb-cli/src/commands/db.js`
- [x] Align parser schema, generated template schema, and workflow library contract
- [x] Resume repeated end-to-end workflow scenarios after schema agreement
- [x] Verify task/session/session_cache regeneration against the agreed schema
- [x] Re-test direct `completeSessionWork()` usage with safe DB open/close behavior

## Dependencies

- T13
- T20
- T20a
