# T21: Database-Native Memory Bank Update Workflow

*Created: 2025-11-13*
*Last Updated: 2026-06-26 01:33:00 IST*

**Status**: 🔄 **IN_PROGRESS**
**Priority**: high

## Details

[Details](tasks/T21.md) - Phase E complete. Phase F: Schema consistency fix COMPLETE. Remaining: downstream propagation, record-only mode, backfill tool.

**2026-06-25 NIGHT — SCHEMA FIX COMPLETE**: Full sweep of mb-core repo fixed all column name mismatches:
- Canonical schema.sql updated to v1.1 (`last_updated`, `session_date`, `session_period`, `focus_task`, `content`)
- All lib files (`inserts.js`, `workflow.js`, `regenerate.js`) aligned to canonical names
- Parser files (`parse-tasks.js`, `parse-sessions.js`) aligned
- CLI commands (`task.js`, `session.js`) aligned
- Template files synced from canonical
- Test data (`test-schema.js`, `generate-test-data.js`) aligned
- Deleted stale `server-package/` and `sync-database-template.js` (would overwrite fixes)
- Created unified schema/protocol reference: `implementation-details/schema-protocol-reference.md`
- Created full audit report: `implementation-details/schema-audit-2026-06-25.md`
- `init-schema.js` verification: PASSED (10 tables, 23 indexes, all canonical columns)

**CRITICAL ISSUE DISCOVERED 2026-06-25 AFTERNOON**: Column name mismatch between schema.sql and lib/inserts.js, lib/workflow.js, lib/regenerate.js. Schema defines `last_updated`, `session_date`, `session_period`, `focus_task` but lib files still reference `updated`, `date`, `period`, `focus`. Also `sessions.content` vs `sessions.notes` mismatch in some repos. Template fix (commit a961805) partially applied to inserts.js and workflow.js but regenerate.js still broken. Multiple stale copies across mb-core and all downstream repos.

**RESOLUTION 2026-06-25 NIGHT**: All mb-core files fixed. Templates are now single source of truth. Old names forbidden. v1.1 naming convention ratified.

## Subtasks

- [x] Create isolated sibling fixture project for Phase E
- [x] Create detailed Phase E test log
- [x] Validate fresh-project bootstrap path
- [x] Fix missing `schema.sql` copy during `mb init --database`
- [x] Fix missing `findDbPath()` helper in `mb-cli/src/commands/db.js`
- [x] Align parser schema, generated template schema, and workflow library contract
- [x] Resume repeated end-to-end workflow scenarios after schema agreement
- [x] Verify task/session/session_cache regeneration against the agreed schema
- [x] Re-test direct `completeSessionWork()` usage with safe DB open/close behavior
- [x] Cross-project validation: Cloudy workspace sync + test + workflow run (Phase F)
- [x] Fix regenerate.js column names (updated → last_updated, date → session_date, period → session_period, focus → focus_task)
- [x] Sync all schema.sql copies in mb-core to canonical version
- [x] Sync mb-core/memory-bank/database/lib/ files to canonical version
- [ ] Fix downstream repos (timesarrow, etc.) - propagate fixed templates
- [ ] Fix mb-cli init behavior to refresh lib files from templates on --force
- [ ] Add CI test to verify schema + lib consistency on commit
- [ ] Record-only mode: add `skip_regeneration` to `recordSessionWork()` and `completeSessionWork()`
- [ ] Full backfill tool: adapt T20 parsers, handle edge cases, verify roundtrip
- [ ] Migration strategy: document text-primary → DB-primary transition criteria and rollback plan

## Dependencies

- T13
- T20
- T20a

